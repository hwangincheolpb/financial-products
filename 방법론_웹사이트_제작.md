# 펀드 비교 웹사이트 제작 방법론

## 📋 프로젝트 개요
- **목적**: 타임폴리오 vs 디에스 사모펀드 비교 웹사이트 제작
- **기술 스택**: HTML, Tailwind CSS, Chart.js
- **특징**: 반응형 디자인, 인터랙티브 차트, 다크 테마

## 🗂️ 파일 구조
```
fund-comparison-web/
├── index.html          # 메인 페이지 (Market Insight)
├── strategy.html       # 전략 & 라인업
├── comparison.html     # 결론 & 배분 전략
├── timefolio.html      # 타임폴리오 상세
├── dsasset.html        # 디에스 상세
└── styles.css          # 공통 스타일
```

## 🎨 디자인 시스템

### 색상 팔레트
```css
/* 배경 */
bg-[#0f172a]           /* 다크 네이비 */
bg-slate-900/50        /* 반투명 슬레이트 */

/* 강조색 */
emerald-400/500/600    /* 타임폴리오 (롱숏) */
blue-500/600           /* 디에스 (롱바이어스) */
amber-400/500          /* 경고/중립 */

/* 텍스트 */
text-white             /* 헤더 */
text-slate-200         /* 본문 */
text-slate-400/500     /* 보조 */
```

### 주요 컴포넌트

#### 1. Glass Panel (유리 효과)
```css
.glass-panel {
    background: rgba(15, 23, 42, 0.6);
    backdrop-filter: blur(12px);
    border: 1px solid rgba(255, 255, 255, 0.1);
}
```

#### 2. Glass Card
```css
.glass-card {
    background: rgba(30, 41, 59, 0.4);
    backdrop-filter: blur(16px);
    border: 1px solid rgba(255, 255, 255, 0.05);
}
```

#### 3. 애니메이션
```css
.animate-fade-in {
    animation: fadeIn 0.6s ease-out;
}

@keyframes fadeIn {
    from { opacity: 0; transform: translateY(20px); }
    to { opacity: 1; transform: translateY(0); }
}
```

## 📊 차트 구현 (Chart.js)

### 기본 설정
```javascript
new Chart(ctx, {
    type: 'line',
    data: {
        labels: ['25.09', '25.10', '25.11', '25.12', '26.01'],
        datasets: [{
            label: 'DS Brave S',
            data: [0, 4.2, 12.8, 22.1, 44.52],
            borderColor: '#10b981',
            borderWidth: 4,
            tension: 0.3,
            fill: true,
            backgroundColor: 'rgba(16, 185, 129, 0.05)'
        }]
    },
    options: {
        responsive: true,
        maintainAspectRatio: false,
        plugins: { legend: { display: false } },
        scales: {
            y: { grid: { color: 'rgba(255,255,255,0.05)' } },
            x: { grid: { display: false } }
        }
    }
});
```

## 🧩 페이지별 구성

### 1. index.html (Market Insight)
- **섹션**: 시장 전망, 2월 전략, 펀드 라인업
- **특징**: 그라데이션 배경, 카드 레이아웃

### 2. strategy.html (Lineup & Choice)
- **섹션**: 자산 배분, 펀드 성과 추적, 신규 라인업
- **특징**: 테이블, 배지, 타임라인

### 3. comparison.html (Conclusion)
- **섹션**: 양사 비교, 배분 전략, 시나리오별 포트폴리오
- **특징**: 2열 레이아웃, 인터랙티브 차트

### 4. timefolio.html / dsasset.html
- **섹션**: 운용 철학, 시스템, 성과, 투자 프로세스
- **특징**: 상세 정보, 다이어그램, 사례 연구

## 🚀 배포 방법

### GitHub Pages 배포
```powershell
# deploy-to-github.ps1 실행
.\deploy-to-github.ps1
```

### 로컬 테스트
```powershell
# HTML 파일 직접 열기
start index.html
```

## 💡 핵심 포인트

### 1. 반응형 디자인
- `max-w-7xl mx-auto`: 최대 너비 제한 + 중앙 정렬
- `grid grid-cols-1 lg:grid-cols-2`: 모바일 1열, 데스크톱 2열
- `hidden md:flex`: 모바일에서 숨김, 태블릿 이상 표시

### 2. 타이포그래피
```css
/* 헤더 */
text-4xl md:text-5xl font-black tracking-tighter uppercase

/* 본문 */
text-sm leading-relaxed font-medium

/* 라벨 */
text-[10px] font-black uppercase tracking-widest
```

### 3. 인터랙션
- `hover:bg-slate-800 transition-colors`: 부드러운 호버 효과
- `hover:scale-105 transition-transform`: 확대 효과
- `cursor-pointer`: 클릭 가능 표시

## 📝 데이터 업데이트 방법

### 1. 펀드 성과 데이터
- 파일: `strategy.html`, `comparison.html`
- 위치: `<tbody>` 태그 내부
- 형식: `<td>수익률</td>`

### 2. 차트 데이터
- 위치: `<script>` 태그 내부
- 수정: `data: [...]` 배열 값 변경

### 3. 시장 전망
- 파일: `index.html`
- 위치: `.text-box` 클래스 내부
- 형식: 마크다운 스타일 리스트

## 🔧 커스터마이징 가이드

### 색상 변경
1. Tailwind 설정에서 색상 정의
2. 모든 페이지에 일관되게 적용
3. 차트 색상도 동일하게 매칭

### 레이아웃 변경
1. `grid-cols-*` 클래스로 열 개수 조정
2. `gap-*` 클래스로 간격 조정
3. `p-*`, `m-*` 클래스로 여백 조정

### 폰트 변경
1. Google Fonts에서 폰트 로드
2. Tailwind 설정에 폰트 추가
3. `font-*` 클래스로 적용

## 📚 참고 자료
- Tailwind CSS: https://tailwindcss.com/docs
- Chart.js: https://www.chartjs.org/docs
- Glassmorphism: https://css.glass/
